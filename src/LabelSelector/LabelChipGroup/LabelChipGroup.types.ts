import { LabelChipProps } from "../LabelChip/LabelChip.types";

/**
 * The props of the LabelChipGroup component.
 */
export type LabelChipGroupProps = {
  /**
   * The array of chips to display in the group. All properties of the LabelChip component are supported.
   */
  chips: LabelChipProps[];
};
