build:
  react-ui:
    image: okteto.dev/react-ui:1.0.0
    context: .
    
deploy:
  commands:
    - docker run ${OKTETO_BUILD_REACT_UI_IMAGE}
    
dev:
  api:
    command: bash
    forward:
      - 6006:6006
