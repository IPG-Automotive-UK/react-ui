build:
  react-ui:
    image: okteto.dev/react-ui:1.0.0
    context: .
    
deploy:
  commands: 
    - okteto deploy --build
    - okteto up
    - npm run storybook

dev:
  api:
    command: ["bash"]
    forward:
      - 6006:6006
