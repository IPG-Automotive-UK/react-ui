import Color from "./Color";
import React from "react";

export default {
  component: Color,
  title: "General/Color"
};

const Template = () => {
  return <Color />;
};

export const Default = Template.bind({});
Default.args = {};
