import React from "react";
import { RoadPreviewProps } from "./RoadPreview.types";

const RoadPreview = (
  {
    name,
    href,
    version,
    image,
    description,
    format,
    formatVersion,
    file,
    createdAt,
    user,
    label
  }: RoadPreviewProps,
  key: React.Key
) => {
  return <></>;
};

export default RoadPreview;
